name: Caregiver WhatsApp Reminder (DISABLED)
# 已停用 - 請使用本地服務 local-reminder-service.js
# 啟用方法：喺本機執行 node local-reminder-service.js

on:
  schedule:
    - cron: '0 * * * *'
  workflow_dispatch:

jobs:
  remind:
    runs-on: ubuntu-latest
    steps:
      - run: echo "DISABLED - use local service local-reminder-service.js"
